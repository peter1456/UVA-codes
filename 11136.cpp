#include <stdio.h>
#include <stdlib.h>

typedef struct NODE{
	struct Node *next;
	int value;
} Node;

long findMax()

int main (){
	int cases, i, j;
	long bills, buffer[100000];
	long max, min;
	Node *head = malloc(sizeof(Node));
	Node->next = NULL;
	scanf("%d",&cases);
	for (i = 0; i < cases; i++){
		scanf("%d",&bills);
		for (j = 0;j < bills,j++){
			scanf("%d",buffer+j);
		}
	}
	for 
}
