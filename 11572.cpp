#include<cstdio>
#include<map>
using namespace std;

int main(){
    map<int, int> data;
    int c, n, tmp, count, max, s[1000005];
    freopen("input.txt", "r", stdin);
    freopen("output.txt", "w", stdout);
    scanf("%d", &c);
    while(c--){
        data.clear();
        max = count = 0;
        scanf("%d", &n);
        for(int i = 0; i < n; i++){
            scanf("%d", s + i);
        }
        
    }
    return 0;
}